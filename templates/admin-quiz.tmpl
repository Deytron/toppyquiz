{{ define "admin-quiz.tmpl" }}
{{ template "header" . }}
<section class="container">
    <h2 class="title is-1 has-text-centered">Ajout d'un quiz</h2>

    <form action="/admin/add-quiz" method="post">
        <div class="field">
            <label class="label">Nom</label>
            <div class="control">
                <input class="input" type="text" name="quiz-name" placeholder="Nom" required>
            </div>
        </div>

        <div class="field">
            <label class="label">Description</label>
            <div class="control">
                <textarea class="textarea" name="quiz-desc" placeholder="Description" required></textarea>
            </div>
        </div>

        <div class="field is-grouped">
            <div class="control">
                <button class="button is-primary" type="submit">Créer le quiz</button>
            </div>
        </div>
    </form>

</section>

<!--  -->
</body>

</html>
{{ end }}